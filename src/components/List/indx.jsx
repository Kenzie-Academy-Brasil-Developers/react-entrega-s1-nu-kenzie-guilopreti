import Card from "../Card";

const List = ({ listTransactions }) => {
  return (
    <ul>
      {listTransactions.map((transaction) => ({
        /*Card*/
      }))}
    </ul>
  );
};

export default List;
